using System;
using System.Collections.Generic;
using System.Text;

namespace DemoConsole.BZ.Contest_2
{
    class PalindromeString
    {
        //static void Main(String[] args)
        //{
        //    string line = Console.ReadLine().Trim();

        //    bool isPal = true;
        //    int l = 0;
        //    int h = line.Length - 1;
        //    while (l <= h)
        //    {

        //        char getAtl = line[l];
        //        char getAth = line[h];

        //        // If there is another symbol  
        //        // in left of sentence 
        //        if (!((getAtl >= 'a' && getAtl <= 'z') || (getAtl >= 'A' && getAtl <= 'Z') || (getAtl >= '0' && getAtl <= '9')))
        //            l++;

        //        // If there is another symbol   
        //        // in right of sentence 
        //        else if (!((getAth >= 'a' && getAth <= 'z') || (getAth >= 'A' && getAth <= 'Z') || (getAth >= '0' && getAth <= '9')))
        //            h--;

        //        // If characters are equal 
        //        else if (getAtl.ToString().ToLower() == getAth.ToString().ToLower())
        //        {
        //            l++;
        //            h--;
        //        }

        //        // If characters are not equal then 
        //        // sentence is not palindrome 
        //        else
        //        {
        //            isPal = false;
        //            break;
        //        }


        //    }

        //    if (isPal)
        //        Console.WriteLine("1");
        //    else
        //        Console.WriteLine("0");
        //}
    }
}
